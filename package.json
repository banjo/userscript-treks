{
    "name": "treks",
    "private": true,
    "version": "0.0.4",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "release": "pnpm run build && git commit -am 'chore: build updates' && bumpp"
    },
    "devDependencies": {
        "@types/jquery": "^3.5.16",
        "bumpp": "^8.2.1",
        "typescript": "^4.9.4",
        "vite": "^4.0.3",
        "vite-plugin-monkey": "^2.10.2"
    }
}
